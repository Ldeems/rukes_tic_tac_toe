<br>
<br>

<%if outcome == "next"%>
    <%try = session[:board].appboard%>
    <%try.each do |a|%>
        <%=a%><br>
    <%end%>
    <br>
    <%if session[:player].player == "o"%>
        <%=session[:p1name]%>s turn
    <%elsif session[:player].player == "x"%>
        <%=session[:p2name]%>s turn 
    <%end%>  
    <br>
    <%open = []%>
    <%session[:board].gboard.each do |a|%>
        <%if a == "o"%>
        <%elsif a == "x"%>
        <%else%>
            <%open << a%>
        <%end%>
    <%end%>
    <br>
    <form method="post" action="update">
    <%open.each do |b|%>
        <input type="radio" name="pick" value="<%=b%>" checked="checked"%><%=b%> 
    <%end%>
    <br>
    <input type="hidden" name="uplayer" value="<%=session[:player].player%>"><%session[:player].player%>
    <input type="submit" name="submit" value="make move" id="button">
    </form> 
<%elsif outcome == "WINNER"%>
    <%if session[:player].player == "x"%>
        <%=session[:p1name]%> is the winner
        <%results = "#{session[:p1name]}"%>
    <%elsif session[:player].player == "o"%>
        <%=session[:p2name]%> is the winner 
        <%results = "#{session[:p2name]}"%>
    <%end%> <br><br>
    <%try = session[:board].appboard%>
    <%try.each do |a|%>
        <%=a%><br>
    <%end%>
    <br>
    <br>
    <%info = Time.new%>
    <%game = "#{session[:p1name]} vs #{session[:p2name]}"%>
    <%add_to_table(game,results,info)%>
    <br>
    <br>
    Start over?<br>
    <form mothod="post" action="playagian">
    <input type="submit" name="" value="start over" id="button">
    </form>
<%elsif outcome == "TIE"%>
    CATS Game<br><br>
    <%try = session[:board].appboard%>
    <%try.each do |a|%>
        <%=a%><br>
    <%end%>
    <br>
    <br>
    <%info = Time.new%>
    <%game = "#{session[:p1name]} vs #{session[:p2name]}"%>
    <%results = "tie game"%>
    <%add_to_table(game,results,info)%>
    <br>
    <br>
    Start over?<br>
    <form mothod="post" action="playagian">
    <input type="submit" name="" value="start over" id="button">
    </form>
<%else%>

broken    
<%end%>        
        



